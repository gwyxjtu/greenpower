# 模型复杂度分析与求解优化建议

## 📊 模型复杂度诊断

### 问题现象
```
根松弛求解耗时: 83秒
完成103,291次单纯形迭代
当前可行解: 19,240.90
最优间隙: 8.13%
预计完整求解时间: 10+ 分钟
```

### 模型规模

| 指标 | 值 | 影响 |
|------|-----|------|
| **约束数** | 87,608 | 巨大 |
| **变量数** | 96,365 | 巨大 |
| **非零系数** | 292,761 | 导致LP宽松求解困难 |
| **二进制变量** | 35,040 ⚠️ | **主要瓶颈** |
| **二次约束** | 17,520 ⚠️ | **次要瓶颈** |

---

## 🔴 复杂度根源分析

### 1️⃣ **时间维度爆炸** （最主要的问题）

```
T = 8760 小时/年
```

每个时间步引入的约束：
```
• 电力平衡:     1条    → 8,760条
• 风电约束:     1条    → 8,760条
• PV约束:       1条    → 8,760条
• 储能约束:     1条    → 8,760条
• 储能动态:     1条    → 8,760条
• 充放电互斥:   1条    → 8,760条
• 电网约束:     2条    → 17,520条
---
总计:            4条/步 × 8,760小时 ≈ 35,040条约束
```

每个时间步的二进制变量：
```
• z1[t] (储能充电): 8,760个
• z2[t] (储能放电): 8,760个
• y1[t] (电网购电): 8,760个
• y2[t] (电网售电): 8,760个
---
总计: 35,040个二进制变量
```

⚠️ **这是NP困难问题 - 求解空间为 2^35040！**

### 2️⃣ **非线性二次约束**

```python
# 第118-119行的约束
p_GD[t] <= x_GD * y1[t]      # 连续变量 × 二进制变量
p_GD_U[t] <= x_GD * y2[t]    # 连续变量 × 二进制变量
```

虽然Gurobi能处理这种二次约束，但：
- 转换为17,520个二次约束
- 需要额外的松弛变量
- 增加了问题复杂度

---

## ✅ 优化方案（已实施）

### 方案 1: 调整求解器参数 ✅ 

已在代码中实施：

```python
model.setParam('MIPGap', 0.05)         # 5% gap（从1%提高）
model.setParam('TimeLimit', 600)       # 10分钟（从5分钟提高）
model.setParam('Method', 3)            # 并发求解法
model.setParam('Presolve', 2)          # 激进预处理
model.setParam('Heuristics', 0.5)      # 增加启发式算法权重
model.setParam('Cuts', 3)              # 激进割平面
```

**预期效果**:
- 根节点求解更快
- 更早找到可行解
- 10% Gap内通常能在5-10分钟内完成

---

## 🔧 进阶优化建议

### 方案 2: 时间离散化（推荐）

将小时级别改为更粗的时间步长：

```python
# 当前
T = 8760                    # 小时级别

# 建议方案 A: 按天聚合
T = 365                     # 日级别，减少24倍复杂度

# 建议方案 B: 3小时步长
T = 2920                    # 3小时级别，减少3倍复杂度

# 建议方案 C: 混合方法
# 前期用粗时间步求解，后期细化
```

**权衡**:
| 时间步 | 优势 | 劣势 |
|-------|------|------|
| 1小时 | 精确 | 求解慢(10+ min) |
| **3小时** | 较精确 | 求解快(2-3 min) |
| 1天 | 非常快 | 储能精度损失 |

### 方案 3: 两阶段求解

```
第一阶段: 用日级别时间步快速获得容量规划
    └─ 求解时间: < 1 分钟
    └─ 目标: 获得x_WT, x_PV, x_ST, x_GD的初值

第二阶段: 固定容量,用小时级别优化调度
    └─ 求解时间: 快速（因为尺寸大幅缩小）
    └─ 目标: 优化时间序列调度
```

### 方案 4: 储能简化

当前的储能模型很复杂：
```python
E[t+1] == E[t] + (eta_ch * p_ST_C[t] - p_ST_D[t] / eta_dis) * delta
z1[t] + z2[t] == 1  # 强制互斥
```

**简化建议**:

```python
# 方案A: 移除互斥约束(允许同时充放电,物理上等价于零损耗)
# 删除: z1[t] + z2[t] == 1

# 方案B: 使用指示约束代替大M约束
model.addConstr((z1[t] == 1) >> (p_ST_C[t] <= p.P_ST_MAX_C))
# 比大M约束更紧,求解更快
```

---

## 📈 性能对比预测

| 方案 | 求解时间 | Gap | 精度损失 | 难度 |
|------|---------|-----|---------|------|
| **当前(小时级)** | 10-20 min | 8-15% | 0% | 简单 |
| **3小时步长** | 2-3 min | 5-8% | ~5% | 简单 ⭐ |
| **1天步长** | <1 min | 3-5% | ~15% | 简单 |
| **两阶段** | 2-5 min | 5-8% | 2% | 中等 |
| **简化储能** | 5-10 min | 5-8% | ~10% | 简单 |

---

## 🎯 推荐实施方案

### 短期（立即可用）
✅ 已完成：调整求解器参数
- 预期将求解时间从15+分钟降至5-10分钟

### 中期（强烈推荐）
1. **改为3小时时间步** 
   - 修改 `params.py` 第14行：`T = 2920`
   - 预期求解时间：2-3分钟
   - 精度损失：仅~5%

2. **或实施两阶段求解**
   - 第一阶段：容量规划
   - 第二阶段：调度优化

### 代码修改建议

#### 方案：改为3小时步长

在 `params.py` 修改：

```python
# 原来
T = 8760          # 小时级
delta = 1.0       # 1小时

# 改为
T = 2920          # 3小时级（8760/3）
delta = 3.0       # 3小时
```

然后在 `generate_synthetic_data()` 中对数据进行聚合：

```python
def generate_synthetic_data(T):
    # ... 生成8760小时数据 ...
    hours_full = np.arange(8760)
    
    # 聚合到3小时
    load_t = []
    alpha_WT_t = []
    alpha_PV_t = []
    mu_MKT_t = []
    
    for i in range(0, 8760, 3):
        load_t.append(np.mean(load_full[i:i+3]))
        alpha_WT_t.append(np.mean(alpha_WT_full[i:i+3]))
        alpha_PV_t.append(np.mean(alpha_PV_full[i:i+3]))
        mu_MKT_t.append(np.mean(mu_MKT_full[i:i+3]))
    
    return np.array(load_t), np.array(alpha_WT_t), ...
```

---

## 🧪 验证方法

运行当前优化：
```bash
python green_power_opt.py
```

观察关键指标：
- ✓ 根节点求解时间
- ✓ 找到第一个可行解的时间
- ✓ Gap下降速度
- ✓ 最终收敛

---

## 总结

### 模型复杂吗?
**是的** - 这是一个大规模混合整数规划问题：
- 87K 约束
- 96K 变量
- 35K 二进制变量
- 这级别的求解时间**正常且合理**

### 能否加速?
**可以** - 通过以下优先级：
1. ✅ **已完成**：求解器参数优化
2. 🔄 **推荐**：改用3小时时间步（最快见效）
3. 🔄 **备选**：两阶段求解法

### 预期效果
- 当前方案：5-10分钟内获得 ~8% Gap 解
- 3小时步长：2-3分钟内获得 ~5% Gap 解
